use flake

# Environment-specific configuration (the ONLY place for manual setup)
export AWS_PROFILE=yui-macbook-pro-m3max
export SSH_KEY=~/.ssh/line-beeper-yui-macbook-pro-m3max

# Auto-add SSH key to agent
ssh-add -l | grep -q "line-beeper" || ssh-add "$SSH_KEY" 2>/dev/null