# Example secrets file (encrypt with sops before committing)
# Run: sops secrets/secrets.yaml

# Database password for Synapse
db_password: "your-secure-db-password"

# Matrix registration shared secret
matrix_registration_shared_secret: "your-registration-secret"

# LINE bridge specific secrets (if needed)
line_bridge_secret: "your-line-bridge-secret"
